HOME := $(shell echo $HOME)
DOTFILES_DIR := $(HOME)/dotfiles
HOME_ZSHRC := $(HOME)/.zshrc
XDG_CONFIG_HOME := $(HOME)/.config
SHELDON_CONFIG := $(CONFIG_HOME)/sheldon

.PHONY init

init: $(HOME_ZSHRC) 

sheldon: 
